services:
  - type: web
    name: volunteer-platform
    env: node
    buildCommand: npm install --legacy-peer-deps && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    # No publish directory needed for Next.js SSR apps - they run as Node.js services
